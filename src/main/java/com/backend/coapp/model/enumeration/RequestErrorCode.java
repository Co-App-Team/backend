package com.backend.coapp.model.enumeration;

/** All possible error codes returned for bad REST API requests. */
public enum RequestErrorCode {
  REQUEST_HAS_NULL_OR_EMPTY_FIELD,
  INVALID_FORMAT_FIELD
}
